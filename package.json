{
  "name": "client-audit-root",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "api": "uvicorn main:app --reload --port 8000 --host 0.0.0.0 --app-dir backend",
    "next": "npm --prefix \"frontend - Next.js\" run dev",
    "vanilla": "npx -y serve -l 5000 frontend-vanilla"
  }
}
